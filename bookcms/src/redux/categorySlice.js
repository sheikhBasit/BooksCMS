// redux/categorySlice.js
import { createSlice } from '@reduxjs/toolkit';

const categorySlice = createSlice({
  name: 'categories',
  initialState: { status: 'Under construction' },
  reducers: {},
});

export default categorySlice.reducer;
